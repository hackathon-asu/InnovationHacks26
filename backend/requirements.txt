fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database
sqlalchemy==2.0.35
greenlet>=3.0.0
asyncpg==0.29.0
alembic==1.13.2
pgvector==0.3.2
psycopg2-binary==2.9.9

# Document parsing — Docling is the primary parser (97.9% table accuracy)
docling>=2.5.0
pypdf==5.4.0                # Basic fallback for when Docling fails

# Healthcare NLP (optional — graceful fallback if not installed)
# scispacy==0.5.4           # Uncomment if scipy/spacy deps resolve
# spacy==3.7.4              # Uncomment if scipy/spacy deps resolve

# AI
google-generativeai==0.8.3

# Utils
pydantic==2.9.2
pydantic-settings==2.5.2
tenacity==9.0.0
httpx==0.27.2
aiofiles==24.1.0
python-dotenv==1.0.1
structlog==24.4.0

# Policy retrieval — web scraping for automated payer policy download
requests==2.32.3            # sync HTTP (used only in non-async helpers)
beautifulsoup4==4.12.3      # HTML parsing for payer index pages
lxml==5.2.2                 # fast HTML/XML parser backend for BS4
anthropic>=0.40.0
